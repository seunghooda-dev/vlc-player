MasterQC V.1.1 - Update Policy
==============================

Goal
----
Updates should replace program files without touching operator data.

Program Folder
--------------
The release folder contains only files needed to run the program:

- MasterQC.exe
- _internal\
- tools\
- mxf_qc_player.ico
- README.txt
- OPERATOR_QUICK_START_KO.txt
- UPDATE_POLICY.txt
- BROADCAST_SAMPLE_CHECKLIST.txt
- BROADCAST_SAMPLE_SET.txt
- smoke_mxf_test.bat
- stability_mxf_test.bat
- ui_layout_check.bat
- preflight_check.bat
- broadcast_sample_validation.bat
- broadcast_sample_set_validation.bat
- broadcast_sample_set_validation.ps1
- backup_release.bat
- rollback_release.bat
- LICENSES\

This folder can be replaced during an update.

Keep MasterQC.exe and _internal\ together. The app is packaged this way
to avoid one-file temporary extraction issues with native Qt/VLC runtime DLLs.

User Data Folder
----------------
Runtime data is stored under:

%LOCALAPPDATA%\MasterQC

This folder contains:

- settings.json
- archive.db
- logs\
- tmp\
- backups\
- reports\

Do not delete or overwrite this folder during normal updates.

Development PC Update
---------------------
On the development PC, use:

update_desktop_release.bat

This rebuilds the package, refreshes the Desktop shortcut, performs a startup
smoke test, and opens the packaged EXE. It does not copy settings.json,
archive.db, logs\, tmp\, or backups\ back into the release folder.

The Desktop shortcut uses mxf_qc_player.ico from the release folder directly.
This avoids stale Windows icon-cache behavior when the EXE icon changes.

Before the release folder is replaced, backup_release.bat copies the current
program folder to:

%LOCALAPPDATA%\MasterQC\backups\release

Target PC Update
----------------
1. Close MasterQC.
2. Replace the release folder with the new package folder.
3. Run preflight_check.bat.
4. Start MasterQC.exe.

Release ZIP files include a build timestamp, for example:

MasterQC V.1.1_20260513_143000.zip

MasterQC V.1.1.zip is also refreshed as the latest-copy alias.

Legacy Data
-----------
Older release folders may contain settings.json, archive.db, logs\, tmp\, or
backups\. The current app reads and writes only %LOCALAPPDATA%. Existing files
in old release folders are preserved and not deleted automatically.

Rollback
--------
If a new EXE has a problem, close the app and run:

rollback_release.bat

The script restores the latest release backup into the release folder. You can
also pass a specific backup folder path. The user data folder stays in place,
so recent file lists, settings, logs, and QC history remain available.
